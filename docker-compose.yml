version: '3.8'

services:
  suhu-app:
    build: .
    container_name: suhu-app-container
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-5000}
    restart: unless-stopped
